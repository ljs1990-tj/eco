<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.UserMapper">
	<select id="selectUser" parameterType="hashmap" resultType="com.example.test1.model.User">
		SELECT *
		FROM ECO_USER
		WHERE USERID = #{userId}
	</select>
	<insert id="insertUser" parameterType="hashmap">
		INSERT INTO ECO_USER
		VALUES
			(#{userId}, #{userPw}, #{name}, #{nickName}, #{gender}, #{phone1}, #{phone2}, #{phone3}, #{email}, #{birth}, 'SILVER', 'Y', 'Y', 0, 0, 0, 'U', NOW(), NOW(), NOW(), DATE_ADD(NOW(), INTERVAL 3 MONTH))
	</insert>
	
	
	
	
	
	
</mapper>