<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.UserMapper">
	<select id="selectUser" parameterType="hashmap" resultType="com.example.test1.model.User">
		SELECT *
		FROM ECO_USER
		WHERE USERID = #{userId}
	</select>
	<insert id="insertUser" parameterType="hashmap">
		INSERT INTO ECO_USER
		VALUES
			(#{userId}, #{userPw}, #{name}, #{nickName}, #{gender}, #{phone1}, #{phone2}, #{phone3}, #{email}, #{birth}, 'SILVER', 'N', #{eventYn}, 0, 0, 0, 'U', NOW(), NOW(), NOW(), DATE_ADD(NOW(), INTERVAL 3 MONTH))
	</insert>
	<update id="updateUser" parameterType="hashmap">
		UPDATE ECO_USER
			SET
		USERPW = #{userPw}, NAME = #{name}, NICKNAME = #{nickName}, GENDER = #{gender}, PHONE1 = #{phone1}, PHONE2 = #{phone2}, PHONE3 = #{phone3}, EMAIL = #{email}, BIRTH = #{birth}
		WHERE USERID = #{userId}
	</update>
	<insert id="insertUserAddr" parameterType="hashmap">
		INSERT INTO ECO_ADDR
			(USERID, ADDR, ADDRDETAIL, ZIPCODE, NAME, PHONE, ADDRREQUEST, ADDRNAME)
		VALUES
			(#{userId}, #{addr}, #{addrDetail}, #{zipCode}, #{name}, #{phone}, NULL, '집')
	</insert>
</mapper>