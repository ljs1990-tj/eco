package com.example.test1.mapper;
import java.util.HashMap;

import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface CartMapper {
	/*
	 * void addToCart(HashMap<String, Object> map); // Adds an item to the cart void
	 * checkoutCart(HashMap<String, Object> map); // Updates IS_PAY to 'Y' for
	 * checkout
	 */}

